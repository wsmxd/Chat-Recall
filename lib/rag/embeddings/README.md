# Embeddings

Embedding provider adapters will live here.

Changing the chat provider should not require rebuilding the knowledge base. Changing the embedding provider may require re-embedding documents.

